@Library(["maif-pipeline-library", "datafactory-pipeline-library"]) _

import fr.maif.ingdev.forge.RunContext

pythonPipeline {
    RunContext.set(
        filiere: 'DATASCIENCE',
        sonarProfileSuffix: 'IODA',
    )
}